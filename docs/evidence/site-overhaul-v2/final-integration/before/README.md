# Before the integration pass

Loaded captures from the starting PR head `ca7893df1315fd484382a515b99c7ffb0fecd199`, using the existing built preview at `http://127.0.0.1:5188/`.

These selected viewport captures preserve the earlier hero/action composition and transition into the Desk. They are focused before evidence, not a consecutive whole-page sequence. The final capture directory supplies the exact consecutive phone sequence and complete desktop/tablet/phone pages after implementation. Earlier broad baseline captures are retained locally but not used as proof that every lazy image had painted.

`alba-continuity-before.json` reproduces the loaded-client `Under Construction` label against directory/compare `Completed` at the starting head. Its static-before-hydration selector returned no matching node, so that subsection is not proof of the SSR label. The previously documented SSR conflict and canonical precedence trace remain separate evidence; final static/no-JS verification is recorded in the final QA memo.
