# Final integration: bounded art direction

Reviewer: GPT-6 Astra, extra-high reasoning. 2026-09-19. Baseline HEAD `ca7893df1315fd484382a515b99c7ffb0fecd199`, `codex/site-overhaul-v2`, initially clean. Reviewed actual built preview `http://127.0.0.1:5188/`; source files were read, not edited. Approved isolated worktree context retained. Both guides, progress, final integration requirements, and previous Desk/hero critiques read. Codebase Memory architecture inspected; no broad repository audit.

## Judgment and scope

Baseline whole-page visual judgment: **7.8/10** (desktop roughly 8.1; phone roughly 7.6). These are subjective critique scores, not conversion or accessibility measurements. The opening image treatment, solid masthead, image-led Desk, featured imagery and original closing illustration form a coherent direction. The page falls short of the above-8 goal because important onward actions are too quiet, several small navigation labels are difficult to read, and inherited typography/spacing rules visibly disrupt the lower sequence. Keep the direction and fix the bounded defects below. No additional imagery, new library or feature is needed.

Screenshots and metrics: `output/playwright/final-integration/baseline/`. Images were captured by this Astra art-direction reviewer to inspect the same browser state and test layout hypotheses; this is a disclosed role overlap from the requested Luna capture allocation, not a model substitution. Luna should perform final implementation captures/regression. All phone evidence is Chromium desktop browser viewport emulation, not physical iPhone testing. Baseline metadata/fonts do not establish WebKit or final-head results.

## Exact evidence

- Full-page captures: `home-{1440,1024,768,390,375,320}-full.png`. Full-page capture was performed after a scrolling pass, but some lazy images can still be incomplete in these tall captures; these are baseline geometry references, not presentation-ready final screenshots. The separately inspected desktop and phone lower-section frames contain the actual loaded images. Final collector must traverse, await every relevant image complete/naturalWidth>0, then capture the full page.
- Complete phone opening sequences: `home-{390,375,320}-01.png` through `-05.png`, ordinary overlapping positions 0,620,1240,1860,2480. These cover masthead, hero, all three Desk stories and first discovery imagery. Width/height verified as1440×1000,1024×900,768×900,390×844,375×812,320×812. No document-level horizontal overflow at these baseline widths.
- Desktop lower reading sequence: `home-1440-02.png` through `-08.png`, inspected through original bridge and footer.
- Phone lower-page inspection: `home-390-lower-1.png` through `-7.png`, collection, compare, NORA, buyer guide, advisory/market preview, bridge/footer.
- `metrics-cli.txt` records baseline geometry. At390/375: masthead111px, hero820px, AskWPB band74px, utility nav59px, Desk starts1064px. At320: masthead115px; Desk starts1068px. Desk height1315/1308/1370px, then discovery begins2379/2372/2438px respectively. Do not remove any of the three previews or their qualifications to shorten this.

## Priority 1: earlier onward paths without squeezing hero

**Observed defect:** Latest's target begins845px at390/375 and849px at320. Its bottom889/893px overlaps the caption target's top886.7/890.7px by2.3px (target boxes; glyphs are not visibly superimposed). It is11px because the specific hero-note rule defeats the intended13px latest-link rule. Compare occupies a separate second row, then Latest a third row with another rule/gap. The result spends vertical space without improving hierarchy.

**Accepted browser-only prototype:** `prototype-{390,375,320}.png`; measurements in `prototype-metrics-cli.txt`. All retain existing headline/body sizes, tower crown, selected image and caption. At all three widths hero is760px, with Latest target755–799px at390/375 and759–803px at320; caption top826.7/830.7 leaves~28px separation. No overflow. This is direction proof, not final source evidence.

Implementation in `src/main.ts` and existing hero rules in `public/assets/styles/v2-editorial.css`:

1. Put the current Latest anchor inside `.v2-hero-actions` as the third anchor. Keep its existing href/tracking identity. Visible labels: `Explore buildings`, `Compare buildings`, `Latest stories`. Concise labels can be consistent across sizes; no need duplicate accessible/focusable links. An aria-label such as `Latest development stories` is optional.
2. Desktop/tablet preserve existing two main actions in first row and latest beneath. Use grid columns `max-content max-content`, existing22px column gap; Latest spans both columns, `justify-self:start`, has its existing separate top rule, but13px type, not11px. Existing desktop composition/crop/hero height remains.
3. At≤720px: `.home-hero { min-height:760px; height:auto }`; `.home-hero-layout { padding-bottom:72px }`; actions grid columns `max-content max-content`, gap `10px 22px`; primary spans both columns and `justify-self:start`; Compare and Latest occupy row2. Both text links13px with44px minimum target,8px arrow gap. Latest resets margin/padding/top-border, uses bottom-rule treatment matching Compare, and has no limiting38ch width. Keep responsive reflow: at increased text scale allow links to wrap/stack rather than preserve fixed max-content width at the expense of overflow. Test200% text and320px; accept taller hero when content requires it.
4. Keep current h1 sizes (46.8px at390,45 at375,40 at320), body16px/1.55, image object-position/derivatives and caption11px. Do not darken or replace the art as part of this pass. The760px target leaves opening longer than one screen with masthead; that is intentional.

## Priority 2: readable secondary navigation and consistent small actions

- `.home-section-jump a` **and its nested span** currently10px, letter spacing. Set13px, weight500, normal/near-normal tracking; min target44px. Retain horizontal scrolling at narrow widths and visible overflow cue; gap24px is sufficient. Do not force seven links into phone width. Use sentence-case `Buyer guides`; label `Buildings` instead of `Projects` for consistency with masthead and actual destination. Preserve each destination.
- `.home-featured-heading-link` displays `View All Projects→` with no arrow separation and10px on phone. Use `View all buildings`,13px, inline-flex gap8px, minimum44px height; at320 allow heading/link to wrap naturally rather than make label tiny.
- `.project-card-actions a` visible copy is `VIEW PROJECT →` despite V2 CSS requesting sentence case. Change displayed homepage action to `View building` (or `View project` consistently),13px; keep data hooks/destination. Shared directory effects must be visually checked.
- Homepage compare button `Compare These Buildings` -> `Compare these buildings`; utility/contact labels should use sentence case consistently. Capitalization remains appropriate for eyebrow treatments and proper names (NORA, North Flagler, Scott Gordon Group).
- Explicit8–10px arrow gaps for `.home-featured-heading-link`, `.home-resource-heading > a`, `.home-resource-card > em`, `.home-brooke-panel > a`, relevant header CTA spans. Keep arrows from visually attaching to last word; preserve icon accessibility. Do not globally rewrite all links or change button hierarchy.
- In lower `.cg-guide`, primary/outlined contact actions currently use much bolder type than other homepage CTAs. Match13px, weight600, line-height1.4 and44–48px height in this touched homepage component, preserving primary/secondary contrast and inquiry links. Keep useful guidance text and factual caveats intact.

## Priority 3: remove visible inherited composition defects

**Corridor heading:** `home-390-04.png` and `home-1440-02.png`. `.corridor-heading` has grid gap24px *plus* h2 top margin12 and deck margin15, creating36/39px internal gaps. Set gap0, retaining12/15px explicit margins. Preserve56px mobile outer section padding and deliberate section pause. Normalize the homepage corridor/collection eyebrows from800 10.88px/1 to600 11px/1.5, matching Desk. This removes accidental heaviness and unnecessary reading delay without removing content.

**Featured gutter:** `home-390-lower-1.png` shows12px outer gutters versus22px in surrounding mobile sections. Source `src/style.css` at~11490 forces12px with!important. Reconcile/remove that obsolete touched declaration so V2's22px gutter controls the feature. Cards, title and images should align to the same22px page grid; do not add another!important layer.

**NORA heading:** `home-390-lower-3.png`: five lines with isolated `NORA` and `District`. `.home-future-module h2` inherits mobile max-width12ch (226.9px) inside a346px column; block spans further force breaks. The textContent also reads `Districtcould` because there is no literal whitespace between spans. Use plain full heading text `Why the NORA District could reshape Downtown.` (same words/factual qualification) or inline spans separated by a real space. Set max-width:none and `text-wrap:balance` in the existing scoped definition; retain34px mobile / current desktop size. Browser-only prototype gives3 lines/114px at390 instead of5/190px, and2 lines/106px at1440 instead of4/213px. `prototype-nora-{390,1440}.png` captures typography; phone prototype was taken before its lazy image finished, so its blank image area is **not** a final visual asset result. Baseline lower-3 proves original image loads; final capture must await it.

**Keep:** Desk's actual lead+two illustrated briefs, all dates/qualification text, image captions, image proportions; compare picker and contextual previews; advisory/market image composition; original full-width bridge and footer. Existing longer buyer-guide section has useful decision content and does not warrant another structural redesign. Homepage market-guide long title/description repeat the same premium-value idea, but this is optional/non-blocking; do not add a brittle one-story override just to shorten it.

## Actual fonts and fallback evidence

No webfonts declared in `document.fonts`. Chromium CDP `CSS.getPlatformFontsForNode` reports **Iowan Old Style Roman** for hero, Desk lead, corridor, compare and NORA headings; **Avenir Next Regular** for hero body; **Avenir Next Medium** for utility navigation. All `isCustomFont:false`. Existing CSS serif fallback is Iowan Old Style → Palatino Linotype → Palatino → Georgia → serif; some inherited scoped headings omit Palatino. Body stack uses Avenir Next → Inter → Segoe UI → sans-serif. Do not claim bundled fonts or identical typography on Windows/Android.

A browser-only Georgia/Arial stress sample (`fallback-georgia-arial-390.png`) changes the hero title from3 lines to2 at390, with no overflow. This is an intentional substitute-font simulation, not a real Windows/device test. Native fallback differences alone are not a blocker; ensure content-driven height and no forced line-break dependence in final source. Preserve installed fonts; no external font service/download needed. Shared font edits, if any, must be scoped and verified on project/article/compare/inquiry routes.

## CSS consolidation and acceptance

Edit existing definitions in `public/assets/styles/v2-editorial.css` rather than append correction overrides. Combine the conflicting `.v2-latest-link` and more-specific `.home-hero .v2-hero-note` typography declarations. Reconcile only touched inherited rules in `src/homepageScopedStyles.ts` (NORA max-width/block spans/compare heading rhythm) and `src/style.css` (mobile featured!important gutter and specific corridor heading gap/weight); do not refactor all stylesheet architecture.

After implementation, inspect actual loaded pages at1440,1024,768,390,375,320, capture consecutive phone frames, and compare against the above evidence. Two focused visual rounds include the fresh final reviewer; do not chase a score by unrelated aesthetic changes. Final reviewer must see full page and interactions. Hero geometry/source fidelity and La Fontana date require separate provenance work; this critique does not close them. Alba canonical-precedence repair endorsed separately: consume existing resolver for status presentation/quick facts, preserve fallback copy and distinct Sales tag, no canonical fact/schemaSafe edit.

No code changes, commits, pushes, merge or deployment by this reviewer. No final acceptance/CI/physical-device claim. This brief is ready for bounded Luna implementation.

## Consequential hero/source review addendum

Inspected the source agent's actual comparison sheets `shorecrest-architect-vs-selected-sheet.png` and `shorecrest-source-vs-selected-crops.png`. These establish that the current warm image should **not** be described as independently architecturally verified. Its broad slender massing, curved front corner and projecting crown are recognizable against the source, but the lower-floor/frontage treatment materially differs: the original shows clearer repeated balcony/terrace edges, a more open recessed/columned ground level, and a different arrangement of low planted beds/arrival space; the selected warm version smooths/changes these details and introduces heavier planting. These are more than a pure color grade. Facade subdivisions also appear changed, but precise floor-count/geometry claims require aligned originals rather than these unregistered raster crops.

The dock/pergola visible in the selected image warrants investigation, but **these cropped sheets alone do not prove that it was invented**: the original developer wide image includes a small structure at the extreme bottom edge, while the original podium crop cuts off much of the waterfront foreground. Do not turn absence from a crop into a factual claim. Obtain/inspect the full developer raster including that edge or another authoritative site view before declaring the dock absent. Even if the dock is supported, the frontage/terrace deviations above keep material accuracy unresolved.

Safest owner-reviewable recommendation: prepare the existing approved original developer EXT02 (if provenance/website approval confirmed) as a fallback with the same responsive composition, caption and only a non-generative warm color/contrast grade. It keeps the established tower/right-hand composition while preserving geometry. If original use is not already approved, present the exact candidate/source and ask the owner to approve it. A fresh generative repair risks repeating the unverified geometry problem. Keep the currently selected hero unchanged in source until that concrete owner decision; retain its AI-assisted caption and record **AWAITING OWNER DECISION** for hero source accuracy. The general rendering label does not close this material discrepancy.

## Lead integration correction after full-source inspection

The lead subsequently inspected the complete official developer EXT02 raster. The dock/pergola is visibly present at lower right; its existence is supported. The earlier cropped comparison was insufficient for that conclusion. See [source verification](source-verification.md) for the complete-file URL, hash and remaining detail differences. The exact original is present in the warehouse public-research lane with **rights review required**, not in the approved-for-website lane. It is a concrete fallback candidate requiring owner/rights approval, not an approved replacement. The selected hero remains unchanged.
