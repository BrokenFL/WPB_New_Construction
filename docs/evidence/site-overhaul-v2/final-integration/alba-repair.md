# Alba source-precedence repair

This implementation note precedes the final frozen-source QA. The canonical status comes from the existing qualified automated projection (`content/overrides/project-fact-automated.json`, `automated_intel`, source as-of June 22, applied September 14 under `p2-fast-policy-v1`). The generated `reviewedFields` name is not proof of a new manual approval. No status, source record or schema-safety approval was changed.

Baseline source: `codex/site-overhaul-v2` at `ca7893df1315fd484382a515b99c7ffb0fecd199`.

`renderEditorialShowcaseProjectPage` now resolves the factual Status tag through `resolveProjectField({ identifier: project.id, field: "status", approvedFallback: copyFactValue(...) })`. Existing configured Status hero tags are mapped to that winner; the existing quick fact is mapped the same way when a showcase supplies one. The repair does not edit canonical data, generated projections, copy packages, schema output, Sales tags, or the existing fact-strip composition. Alba's reviewed public winner remains `Completed`; the stale copy fallback is no longer rendered in the showcase Status tag.

The focused AST-presenter fixture suite is `research/scripts/homepage-news-fixtures.test.mjs` and passes 6/6. It also guards the count-neutral Desk footer for one/two story fixtures. The browser gap runner is `research/scripts/check-final-integration-gaps.mjs` (`npm run qa:final-integration-gaps`) and records direct, reload, directory-card, SPA/back, compare, article direct/reload/back, isolated long-title/no-image layout, static hero semantics, reduced-motion, and simulated text-enlargement checks without sending leads or analytics. The current two-engine dev run at `http://127.0.0.1:5186` passed 54/54 checks across 1440/390/320. The project form exercises an intercepted 503, retry, intercepted 200 receipt, retained Alba/interest context, consent reset, and two captured payloads. Corrected mobile masthead rules wrap navigation labels and stack the brand at 320px; the runner asserts bounds, overlap, and scroll-width conditions. Screenshots are `output/playwright/final-integration/text-stress-390.png` and `text-stress-320.png`.

Raw JSON-LD status remains intentionally omitted and is recorded as an existing safety boundary. The current before continuity proof is `output/playwright/final-integration/before/alba-continuity-before.json`, with intercepted external mutations and no real lead/analytics submission.
